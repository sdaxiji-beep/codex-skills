{
  "task": {
    "id": "task-132-lab-apply-requires-preview",
    "title": "Require dry-run preview before applying imported report",
    "type": "refactor"
  },
  "target": {
    "files": [
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.js",
        "action": "modify"
      },
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.wxml",
        "action": "modify"
      }
    ],
    "deploy": {
      "type": "none",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "Guard imported report apply so users must run dry-run preview before applying changes to editor state.",
    "acceptance": [
      "Apply button is disabled until dry-run preview exists",
      "Apply method blocks with guidance message when preview has not been generated",
      "Existing undo flow remains unchanged"
    ],
    "constraints": [
      "Only modify the lab project under G:\\codex测试",
      "Do not change production project files",
      "Keep validation local to modified lab files"
    ]
  },
  "validation": {
    "layer": 0,
    "command": "Push-Location 'G:\\codex测试\\miniprogram\\pages\\lab'; node --check '.\\\\index.js'; Pop-Location"
  },
  "record": {
    "enabled": true,
    "output_path": "artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "restore_target_files",
    "on_deploy_fail": "notify_only",
    "on_write_fail": "abort"
  },
  "auto_mode": true
}
