{
  "task": {
    "id": "task-003",
    "title": "Generate preview QR for current project version",
    "type": "preview"
  },
  "target": {
    "files": [],
    "deploy": {
      "type": "preview",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "Generate a preview QR only. Do not modify code and do not deploy cloud functions.",
    "acceptance": [
      "Preview QR generation succeeds",
      "No business code is modified"
    ],
    "constraints": [
      "Read-only operation"
    ]
  },
  "validation": {
    "layer": 4,
    "required_tests": []
  },
  "record": {
    "enabled": true,
    "output_path": "artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "abort",
    "on_deploy_fail": "notify_only"
  },
  "auto_mode": false
}
