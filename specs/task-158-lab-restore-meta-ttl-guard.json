{
  "task": {
    "id": "task-158",
    "title": "Lab add restore metadata TTL guard",
    "type": "feature"
  },
  "target": {
    "files": [
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.js",
        "action": "modify"
      },
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.wxml",
        "action": "modify"
      }
    ],
    "deploy": {
      "type": "none",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "Add TTL-based freshness check for restore metadata, show stale/fresh state in UI, and block restore-metadata export when stale.",
    "acceptance": [
      "Restore metadata tracks age and stale flag with default TTL of 10 minutes",
      "Imported validation card shows restore freshness state with age and TTL",
      "Restore diff export actions are blocked when metadata is stale"
    ],
    "constraints": [
      "Only modify G:\\codex测试",
      "Never touch D:\\卤味"
    ]
  },
  "validation": {
    "layer": 4,
    "required_tests": []
  },
  "record": {
    "enabled": true,
    "output_path": "G:\\codex专属\\artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "restore_target_files",
    "on_deploy_fail": "notify_only",
    "on_write_fail": "abort"
  },
  "auto_mode": false
}
