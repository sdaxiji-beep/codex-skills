{
  "task": {
    "id": "task-134-lab-apply-noop-guard",
    "title": "Block imported apply when dry-run has no changes",
    "type": "refactor"
  },
  "target": {
    "files": [
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.js",
        "action": "modify"
      },
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.wxml",
        "action": "modify"
      }
    ],
    "deploy": {
      "type": "none",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "When dry-run reports zero changed fields, disable apply action and show no-op guidance if apply is attempted.",
    "acceptance": [
      "Apply button is disabled when changedCount is 0",
      "applyImportedReportToEditor exits with no-op guidance when changedCount is 0",
      "Existing apply/undo behavior remains unchanged for changedCount > 0"
    ],
    "constraints": [
      "Only modify the lab project under G:\\codex测试",
      "Do not change production project files",
      "Keep validation local to modified lab files"
    ]
  },
  "validation": {
    "layer": 0,
    "command": "Push-Location 'G:\\codex测试\\miniprogram\\pages\\lab'; node --check '.\\\\index.js'; Pop-Location"
  },
  "record": {
    "enabled": true,
    "output_path": "artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "restore_target_files",
    "on_deploy_fail": "notify_only",
    "on_write_fail": "abort"
  },
  "auto_mode": true
}
