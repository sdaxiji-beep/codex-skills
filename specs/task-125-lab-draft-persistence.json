{
  "task": {
    "id": "task-125-lab-draft-persistence",
    "title": "Persist lab draft input and preset selection",
    "type": "feature"
  },
  "target": {
    "files": [
      {
        "path": "G:\\codex测试\\miniprogram\\pages\\lab\\index.js",
        "action": "modify"
      }
    ],
    "deploy": {
      "type": "none",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "Persist the lab page draft so inputValue and selectedPreset survive page re-entry inside the G:\\codex测试 lab project.",
    "acceptance": [
      "The lab page restores the last saved inputValue on load",
      "The lab page restores the last saved selectedPreset when it still exists",
      "Preset and input changes update the saved draft without touching D:\\卤味"
    ],
    "constraints": [
      "Only modify the lab project under G:\\codex测试",
      "Do not change production project files",
      "Keep validation local to the modified lab page file"
    ]
  },
  "validation": {
    "layer": 0,
    "command": "Push-Location 'G:\\codex测试\\miniprogram\\pages\\lab'; node --check '.\\\\index.js'; Pop-Location"
  },
  "record": {
    "enabled": true,
    "output_path": "artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "restore_target_files",
    "on_deploy_fail": "notify_only",
    "on_write_fail": "abort"
  },
  "auto_mode": true
}
