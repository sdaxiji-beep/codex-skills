{
  "task": {
    "id": "task-007-cloud-changed",
    "title": "Deploy changed cloud functions only",
    "type": "deploy"
  },
  "target": {
    "files": [],
    "deploy": {
      "type": "cloud-changed",
      "functions": [],
      "version": "1.0.0"
    }
  },
  "requirements": {
    "description": "Deploy only the cloud functions currently detected as changed by git status, without modifying any additional business code.",
    "acceptance": [
      "Validation passes before deploy",
      "Only changed cloud functions are targeted",
      "No new business code changes are introduced"
    ],
    "constraints": [
      "Do not modify business code in this task",
      "Reuse only the already existing tracked cloud function change"
    ]
  },
  "validation": {
    "layer": 4,
    "required_tests": []
  },
  "record": {
    "enabled": true,
    "output_path": "artifacts\\records"
  },
  "rollback": {
    "on_validate_fail": "abort",
    "on_deploy_fail": "notify_only"
  },
  "auto_mode": false,
  "auto_write": true,
  "auto_deploy": true
}
